{
  "name": "martketplace-nodejs-firebase",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "node server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "@appbaseio/reactivesearch": "^3.11.6",
    "express": "^4.16.2",
    "firebase": "^7.15.5"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
